#include "Header.h"

void IsValid(const string EQ,
			 bool  isMatch)
{
	if(isMatch)
	{
		cout << endl << EQ << " is valid\n";
	}
	else
	{
		cout << endl << EQ << " is NOT valid\n";
	}
}
